<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IslandPostPOS.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IslandPostPOS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <!-- Background image -->
        <Image Source="/Assets/login_backgorund.jpg"
           Stretch="UniformToFill"/>

        <!-- Black gradient overlay -->
        <Rectangle>
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <!-- Semi-transparent black at top -->
                    <GradientStop Color="#CC000000" Offset="0"/>
                    <!-- Fully transparent at bottom -->
                    <GradientStop Color="#F5000000" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>


        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Image Source="/Assets/TheIslandPostLogo.png" Margin="0,0,0,40"/>
            <TextBox Grid.Row="1" x:Name="UsernameBox" PlaceholderText="Username" Margin="0,0,0,10" Text="{x:Bind ViewModel.Username, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
            <PasswordBox Grid.Row="2" x:Name="PasswordBox" PlaceholderText="Password" PasswordChanged="PasswordBox_PasswordChanged"/>
            <CheckBox Grid.Row="3" Content="Remember Me" IsChecked="{x:Bind ViewModel.KeepLoggedIn, Mode=TwoWay}" Foreground="White" Margin="0,0,0,20"/>
           
            <Grid Grid.Row="4">
                <Button Content="Login" Command="{x:Bind ViewModel.LoginCommand}" HorizontalAlignment="Stretch" Background="White"/>
                <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"/>
            </Grid>
            <TextBlock Grid.Row="5" Text="{x:Bind ViewModel.StatusMessage,Mode=OneWay}" Foreground="White" HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>
