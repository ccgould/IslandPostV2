<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IslandPostPOS.Views.LoadingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IslandPostPOS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <!-- Background image -->
        <Image Source="/Assets/login_backgorund.jpg" Stretch="UniformToFill"/>

        <!-- Black gradient overlay -->
        <Rectangle>
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <!-- Semi-transparent black at top -->
                    <GradientStop Color="#CC000000" Offset="0"/>
                    <!-- Fully transparent at bottom -->
                    <GradientStop Color="#F5000000" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Image Source="/Assets/TheIslandPostLogo.png" Margin="0,0,0,40"/>
            <ProgressBar Grid.Row="1" Minimum="0" Maximum="100" Value="{x:Bind ViewModel.ProgressValue,Mode=OneWay}"/>
            <TextBlock Grid.Row="2" Text="{x:Bind ViewModel.StatusMessage,Mode=OneWay}" Margin="0,20,0,0" Foreground="White" HorizontalTextAlignment="Center"/>
            <ProgressRing Grid.Row="3" IsActive="{x:Bind ViewModel.IsLoading,Mode=OneWay}" Width="20" Height="20"/>
        </Grid>
    </Grid>
</Page>
