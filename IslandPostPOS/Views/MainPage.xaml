<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IslandPostPOS.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IslandPostPOS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  TitleBar  -->
            <RowDefinition Height="*" />
            <!--  NavigationView  -->
        </Grid.RowDefinitions>

        <TitleBar
                x:Name="titleBar"
                Title="Island Post POS"
                Subtitle="V 1.0.0"
                IsBackButtonVisible="False"
                IsPaneToggleButtonVisible="False">
            <TitleBar.LeftHeader>
                <ImageIcon
                                Height="16"
                                Margin="16,0,8,0"
                                Source="/Assets/Tiles/logo.ico" />
            </TitleBar.LeftHeader>
            <TitleBar.RightHeader>
                <Button>
                    <Button.Content>
                        <PersonPicture Width="30"
                       Height="30"
                       DisplayName="{x:Bind Service.CurrentUser.Name}"/>
                    </Button.Content>
                    <Button.Flyout>
                        <MenuFlyout>
                            <!-- Header -->
                            <MenuFlyoutItem Text="The Island Post" IsEnabled="False">
                                <MenuFlyoutItem.Icon>
                                    <BitmapIcon UriSource="ms-appx:///Assets/user_icon.png"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <!-- Divider -->
                            <MenuFlyoutSeparator/>

                            <!-- Account Management -->
                            <MenuFlyoutItem Text="Manage Account and Devices">
                                <MenuFlyoutItem.Icon>
                                    <SymbolIcon Symbol="Contact"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutItem Text="Payment Methods">
                                <MenuFlyoutItem.Icon>
                                    <SymbolIcon Symbol="Account"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutItem Text="Redeem Code or Gift Cards">
                                <MenuFlyoutItem.Icon>
                                    <SymbolIcon Symbol="Shop"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <!-- Divider -->
                            <MenuFlyoutSeparator/>

                            <!-- Support -->
                            <MenuFlyoutItem Text="Help with Payments and Refunds">
                                <MenuFlyoutItem.Icon>
                                    <SymbolIcon Symbol="Help"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutItem Text="Send Feedback">
                                <MenuFlyoutItem.Icon>
                                    <SymbolIcon Symbol="Message"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutItem Text="Store Settings">
                                <MenuFlyoutItem.Icon>
                                    <SymbolIcon Symbol="Setting"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <!-- Divider -->
                            <MenuFlyoutSeparator/>

                            <!-- Sign Out -->
                            <MenuFlyoutItem Text="Sign Out" Click="SignOut_Click">
                                <MenuFlyoutItem.Icon>
                                    <SymbolIcon Symbol="Cancel"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>

            </TitleBar.RightHeader>
            <TitleBar.Content>
                <AutoSuggestBox
                                Width="360"
                                VerticalAlignment="Center"
                                PlaceholderText="Search.."
                                QueryIcon="Find" />
            </TitleBar.Content>
        </TitleBar>


        <NavigationView x:Name="NavView"
                Grid.Row="1"
                PaneTitle="Island Post POS"
                IsBackButtonVisible="Collapsed"
                IsSettingsVisible="True"
                PaneDisplayMode="LeftCompact"
                IsPaneToggleButtonVisible="False"
                IsTabStop="False"
                SelectionChanged="NavView_SelectionChanged">

    <NavigationView.MenuItems>
        <NavigationViewItem Content="Home" Icon="Home" Tag="home"/>
        <NavigationViewItem Content="Profile" Icon="Contact" Tag="profile"/>
        <NavigationViewItem x:Name="storeItem" Content="Store" Icon="Shop" Tag="store"/>
        <NavigationViewItem x:Name="inventoryItem" Content="Inventory" Icon="Add"/>
    </NavigationView.MenuItems>

    <Frame x:Name="ContentFrame"/>
</NavigationView>
    </Grid>
</Page>
