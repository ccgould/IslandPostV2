<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="IslandPostPOS.Views.ManagerCategoriesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IslandPostPOS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dataGrid="using:Syncfusion.UI.Xaml.DataGrid"
    xmlns:converters="using:IslandPostPOS.Converters"
    xmlns:winConverters="using:CommunityToolkit.WinUI.Converters"
    xmlns:controls="using:IslandPostPOS.Views.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:IntToBoolConverter x:Key="IntToBool"/>
        <winConverters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:ByteArrayToImageConverter x:Key="ByteArrayToImage"/>
    </Page.Resources>

    <Grid>
        <Grid Visibility="{x:Bind ViewModel.IsEditing, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=True, Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!--Header-->
            <TextBlock Text="Product Categories" Margin="10,20,0,20" FontWeight="Bold" FontSize="25"/>
            <!--Info and Import,Export Buttons-->
            <Grid  Grid.Row="1"  Background="LightGray" Height="100" ColumnSpacing="5" Padding="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="A list of all of all your product categories." FontWeight="SemiLight" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <Button Grid.Column="2" Content="Add Category" Style="{StaticResource PayButtonStyle}" Command="{x:Bind ViewModel.AddNewProductCommand}"/>
            </Grid>
            <ScrollViewer Grid.Row="2" Padding="20">
                <Grid Padding="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!--Datagrid-->
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <dataGrid:SfDataGrid x:Name="dataGrid"
                                     SelectionMode="None"
                                     AllowGrouping="True"
                                     ItemsSource="{x:Bind ViewModel.service.Categories}"
                                     EnableDataVirtualization="True"
                                     AllowSorting="True"
                                     AllowFiltering="True"
                                     ShowGroupDropArea="True"
                                     Height="300"
                                     DataContext="{x:Bind ViewModel}"
                                     AutoGenerateColumns="False">
                            <dataGrid:SfDataGrid.Columns>
                                <dataGrid:GridTextColumn MappingName="Description"
                                                 HeaderText="Description"
                                                 ColumnWidthMode="Star"/>
                                <dataGrid:GridCheckBoxColumn MappingName="IsActive"
                                                 HeaderText="IsActive"
                                                 ValueBinding="{Binding IsActive,Converter={StaticResource IntToBool}}"/>
                                <dataGrid:GridTemplateColumn HeaderText="Actions" Width="150">
                                    <dataGrid:GridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="5">
                                                <Button Content="Edit"
                        Style="{StaticResource PayButtonStyle}"
                        Command="{Binding DataContext.EditCommand, ElementName=dataGrid}"
                        CommandParameter="{Binding}" 
                        Width="60" Height="30"/>
                                                <Button Content="Delete"
                        Style="{StaticResource PayButtonStyle}"
                        Command="{Binding DataContext.DeleteCommand, ElementName=dataGrid}"
                        CommandParameter="{Binding}" 
                        Width="60" Height="30"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </dataGrid:GridTemplateColumn.CellTemplate>
                                </dataGrid:GridTemplateColumn>

                            </dataGrid:SfDataGrid.Columns>
                        </dataGrid:SfDataGrid>
                    </Grid>
                </Grid>
            </ScrollViewer>
        </Grid>
        <Grid Visibility="{x:Bind ViewModel.IsEditing, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!--Header-->
            <StackPanel Orientation="Horizontal" Spacing="5">
                <Button BorderThickness="0" x:Name="backBTN" Command="{x:Bind ViewModel.BackCommand}">
                    <SymbolIcon Symbol="Back"/>
                </Button>
                <TextBlock x:Name="editNewTxtBox" Text="New Category" Margin="10,20,0,20" FontWeight="Bold" FontSize="25"/>
            </StackPanel>
            <!--Info and Import,Export Buttons-->
            <Grid  Grid.Row="1"  Background="LightGray" Height="100" ColumnSpacing="5" Padding="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Add new product category" FontWeight="SemiLight" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <Button Grid.Column="1" Content="Cancel" Style="{StaticResource PayButtonStyle}" />
                <Button Grid.Column="2" Content="Save" Style="{StaticResource PayButtonStyle}" Command="{x:Bind ViewModel.SaveCommand}"/>
            </Grid>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical" Margin="30,30,0,0">
                    <TextBlock Text="Catergories" FontWeight="Bold" FontSize="15"/>
                    <TextBlock TextWrapping="WrapWholeWords" Text="Create Category page: quickly add new category name, thumbnail, pricing, tax settings, visibility, and shortcuts, permissions."/>
                </StackPanel>
                <Grid Grid.Column="1" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="CATEGORY INFORMATION" FontWeight="Bold" FontSize="20"/>
                    <Rectangle 
                        Grid.Row="1"
                        Height="1" 
                        Fill="Gray" 
                        HorizontalAlignment="Stretch" 
                        Margin="0,8,0,8"/>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Name:" VerticalAlignment="Center" FontWeight="Bold"/>
                        <TextBox Grid.Column="1" Margin="10,0" HorizontalAlignment="Left" Text="{x:Bind ViewModel.CurrentCategory.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Page>
