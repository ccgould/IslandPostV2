<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="IslandPostPOS.Windows.LoadingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IslandPostPOS.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:IslandPostPOS.ViewModels"
    mc:Ignorable="d"
    Title="Loading"
    >

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <ProgressRing IsActive="{Binding IsLoading}" Width="60" Height="60"/>
            <TextBlock Text="{Binding StatusMessage}" Margin="0,20,0,0"/>
            <ProgressBar Minimum="0" Maximum="100"
                         Value="{Binding ProgressValue}"
                         Width="200" Height="20"/>
        </StackPanel>
    </Grid>


</Window>
